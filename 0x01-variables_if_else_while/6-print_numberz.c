#include <stdio.h>

/**
 * main - prints single digits of base 10 starting from 0
 *
 * Return: Always 0 on success
 */
int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
